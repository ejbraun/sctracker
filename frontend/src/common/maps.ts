// The only currently-supported map (specs/backend/01 — maps is a curated, well-defined set;
// /upload-run rejects any upload for a map_id not seeded there). Shared by Dashboard's map picker
// and Run History's map filter so both default to it consistently.
export const DEFAULT_MAP_ID = '72';
