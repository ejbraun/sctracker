<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.5.4</version>
        <relativePath/>
    </parent>

    <groupId>com.howl</groupId>
    <artifactId>uwtracker</artifactId>
    <version>1.0-SNAPSHOT</version>

    <properties>
        <java.version>25</java.version>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <testcontainers.version>1.21.4</testcontainers.version>
        <google-cloud-libraries-bom.version>26.87.0</google-cloud-libraries-bom.version>
    </properties>

    <!-- Only here to version-align google-cloud-storage's large transitive tree (gRPC, protobuf,
         gax, opencensus). spring-boot-starter-parent still manages the handful that overlap
         (protobuf, guava) — its versions win unless deliberately re-pinned. -->
    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>com.google.cloud</groupId>
                <artifactId>libraries-bom</artifactId>
                <version>${google-cloud-libraries-bom.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>
        <dependency>
            <groupId>org.liquibase</groupId>
            <artifactId>liquibase-core</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-crypto</artifactId>
        </dependency>
        <!-- Persists HttpSession rows in MySQL so a redeploy / node restart doesn't log everyone
             out, and so sessions still work if Cloud Run ever runs more than one instance. Schema
             is owned by Liquibase (changeset 040), not Spring's schema initializer
             (spring.session.jdbc.initialize-schema=never). Version managed by spring-boot-parent. -->
        <dependency>
            <groupId>org.springframework.session</groupId>
            <artifactId>spring-session-jdbc</artifactId>
        </dependency>
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
        </dependency>
        <!-- Cloud Run -> Cloud SQL over a Unix socket (no public IP exposure) — see
             docs/deploy plan. Artifact must be the -connector-j-8 variant to match the
             mysql-connector-j 8.x dependency above. -->
        <dependency>
            <groupId>com.google.cloud.sql</groupId>
            <artifactId>mysql-socket-factory-connector-j-8</artifactId>
            <version>1.28.4</version>
        </dependency>
        <!-- Fetches SCTracker.dll + SCTracker.version.json from a private GCS bucket at runtime
             (see com.howl.uwtracker.plugin.GcsPluginStorageClient) instead of the binaries being
             baked into the image. Uses ADC — the Cloud Run runtime service account in prod,
             GOOGLE_APPLICATION_CREDENTIALS / `gcloud auth application-default login` locally.
             Version from libraries-bom above. Note: pulls a large transitive tree (gRPC, Netty). -->
        <dependency>
            <groupId>com.google.cloud</groupId>
            <artifactId>google-cloud-storage</artifactId>
        </dependency>
        <!-- JSON console logs so Cloud Run's log agent parses each line into jsonPayload (queryable
             fields) instead of one opaque textPayload string, and maps our level to Cloud Logging's
             severity instead of every line landing as INFO regardless of actual level. -->
        <dependency>
            <groupId>net.logstash.logback</groupId>
            <artifactId>logstash-logback-encoder</artifactId>
            <version>8.0</version>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-testcontainers</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>mysql</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>

</project>
