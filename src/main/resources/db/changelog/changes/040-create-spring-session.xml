<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">

    <!-- Backing store for Spring Session JDBC (spring.session.jdbc.initialize-schema=never, so this
         is the only thing that creates these tables). Column names, types, index names and the
         cascade are a faithful copy of spring-session-jdbc's own org/springframework/session/jdbc/
         schema-mysql.sql for the version pulled by spring-boot 3.5 — kept in sync by hand here
         rather than sourced from the jar so a library bump can't silently reshape the schema
         underneath an already-applied changeset. mysql:8.4 defaults ENGINE to InnoDB and
         ROW_FORMAT to DYNAMIC, so the script's explicit clauses for those are omitted.

         Uppercase table names are what the library's SQL uses verbatim; on our case-sensitive
         Linux MySQL they must be created with exactly this casing. -->
    <changeSet id="040-create-spring-session" author="uwtracker">
        <createTable tableName="SPRING_SESSION">
            <column name="PRIMARY_ID" type="CHAR(36)">
                <constraints primaryKey="true" primaryKeyName="SPRING_SESSION_PK" nullable="false"/>
            </column>
            <column name="SESSION_ID" type="CHAR(36)">
                <constraints nullable="false"/>
            </column>
            <column name="CREATION_TIME" type="BIGINT">
                <constraints nullable="false"/>
            </column>
            <column name="LAST_ACCESS_TIME" type="BIGINT">
                <constraints nullable="false"/>
            </column>
            <column name="MAX_INACTIVE_INTERVAL" type="INT">
                <constraints nullable="false"/>
            </column>
            <column name="EXPIRY_TIME" type="BIGINT">
                <constraints nullable="false"/>
            </column>
            <column name="PRINCIPAL_NAME" type="VARCHAR(100)"/>
        </createTable>
        <createIndex indexName="SPRING_SESSION_IX1" tableName="SPRING_SESSION" unique="true">
            <column name="SESSION_ID"/>
        </createIndex>
        <createIndex indexName="SPRING_SESSION_IX2" tableName="SPRING_SESSION">
            <column name="EXPIRY_TIME"/>
        </createIndex>
        <createIndex indexName="SPRING_SESSION_IX3" tableName="SPRING_SESSION">
            <column name="PRINCIPAL_NAME"/>
        </createIndex>

        <createTable tableName="SPRING_SESSION_ATTRIBUTES">
            <column name="SESSION_PRIMARY_ID" type="CHAR(36)">
                <constraints nullable="false"/>
            </column>
            <column name="ATTRIBUTE_NAME" type="VARCHAR(200)">
                <constraints nullable="false"/>
            </column>
            <column name="ATTRIBUTE_BYTES" type="BLOB">
                <constraints nullable="false"/>
            </column>
        </createTable>
        <addPrimaryKey tableName="SPRING_SESSION_ATTRIBUTES" columnNames="SESSION_PRIMARY_ID, ATTRIBUTE_NAME"
                       constraintName="SPRING_SESSION_ATTRIBUTES_PK"/>
        <addForeignKeyConstraint baseTableName="SPRING_SESSION_ATTRIBUTES" baseColumnNames="SESSION_PRIMARY_ID"
                                 constraintName="SPRING_SESSION_ATTRIBUTES_FK"
                                 referencedTableName="SPRING_SESSION" referencedColumnNames="PRIMARY_ID"
                                 onDelete="CASCADE"/>
    </changeSet>

</databaseChangeLog>
