package com.howl.uwtracker.admin.dto;

/** How many runs currently match AdminRunController's wipe criteria — shown before the button fires. */
public record UnregisteredRunsCountResponse(long count) {
}
